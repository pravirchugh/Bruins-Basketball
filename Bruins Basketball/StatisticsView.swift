import SwiftUI

struct StatisticsView: View {
    

    
    var body: some View {
        
        VStack {
            HStack {
                // two buttons (switch between)
            }
            
            // content from web scraper
        }

    }
}
